# Data Source Properties
spring:
  # Email Sending
  mail:
    host: smtp.test.com
    port: 587
    username: ${MAIL_USER:speedywheels@test.com}
    password: ${MAIL_PASSWORD:2222}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Email Recipient
email:
  recipient:
    address: ${EMAIL_RECIPIENT:test@gmail.com}


# Cloudinary
cloudinary:
  name: ${CLOUDINARY_NAME:test-cloud}
  api-key: ${CLOUDINARY_API_KEY:test-api-key}
  api-secret: ${CLOUDINARY_API_SECRET:test-secret}

# Administrator
admin:
  username: ${ADMIN_USERNAME:venci777}
  password: ${ADMIN_PASSWORD:sssss}
  first-name: ${ADMIN_FIRST_NAME:Ventsialav}
  last-name: ${ADMIN_LAST_NAME:Stoyanov}
  email: ${ADMIN_EMAIL:admin@example.com}
  age: ${ADMIN_AGE:28}
  city: ${ADMIN_CITY:Sofia}
  phone-number: ${ADMIN_PHONE_NUMBER:1234567890}
  profile-picture: ${ADMIN_PROFILE_PICTURE:https://res.cloudinary.com/db1rc9fon/image/upload/v1721171947/IMG_2073_jrmc8n.jpg}